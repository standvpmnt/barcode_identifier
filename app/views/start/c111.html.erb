<h1>Scan the QR code</h1>
<%= form_with(url: start_c111_add_path, method: "get", local: true) do %>
	<div><%= label_tag ("Check this: ")%>
	<%= text_field_tag :bcode,'', autofocus: true %></div>
	<%= submit_tag ("Search") %>
<% end %>